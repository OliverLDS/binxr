# (Optional) Coerce account to data.frames

(Optional) Coerce account to data.frames

## Usage

``` r
coerce_account_frames(account)
```

## Arguments

- account:

  get_account() result

## Value

list(assets=..., positions=...)
